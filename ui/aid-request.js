(()=>{const prev=window.productionUI;window.productionUI={...prev,modal(c){if(c.s.modal!=='aidRequest')return prev.modal(c);const {s,img,btn}=c;return `<div class="aid-material-picker">${MoleReview.ITEMS.map((it,i)=>s.inventory[i]>0&&i!==6?`<button data-action="extra.aid.material.select" data-payload="${i}" aria-pressed="${s.aidDraft===i}" class="${s.aidDraft===i?'selected':''}">${img(it[0])}<strong>${it[1]}</strong><small>拥有 ${s.inventory[i]}</small></button>`:'').join('')}</div><p class="aid-request-estimate">申请数量 5 · 预计12分钟完成</p><div class="actions">${btn('确认发布','extra.aid.request.submit')}</div>`;}};})();
